{
  "name": "delivery-eta-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend/my-react-app && npm run build --prefix frontend/my-react-app"
  }
}
